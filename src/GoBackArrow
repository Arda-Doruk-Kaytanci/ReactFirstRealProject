import React, { useEffect, useState } from "react";
import { Route, Routes, Link, BrowserRouter, useLocation } from "react-router-dom";
import Homepage from "./Homepage";
import Nav from "./Nav";
import usePrevious from "./UsePrevious";

function GoBackArrow() {
    const [currSite, setcurrSite] = useState(useLocation());
    const [currElement, setcurrElement] = useState(<Homepage />);
    const prevSite = usePrevious(currSite)
    const prevElement = usePrevious(currElement)
    useEffect(()=>{
        
    }, [Nav])
    function onClickHandler() {
    }
    return
    (
        <div>

            <BrowserRouter>
                <Link to={currSite}></Link>
                <Route path={currSite} element={currElement}></Route>
            </BrowserRouter>
        </div>
    );
}
export default GoBackArrow